!!!
%html
  %head
    %title Squash Specs
    = csrf_meta_tags 
    = csp_meta_tag
    = favicon_link_tag
    = stylesheet_pack_tag 'application'
    = javascript_include_tag 'https://kit.fontawesome.com/dc672ed210.js', crossorigin: "anonymous" 
    = javascript_pack_tag 'application'

  %body
    - flash.each do |message_type, message|
      = content_tag(:p, message, id: "text_med", class: "alert alert-#{message_type}") 

    = yield
